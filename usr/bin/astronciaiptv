#!/bin/sh
cd "$(dirname "$(readlink -f "$0")")/../lib/astronciaiptv/" || exit 1
LD_LIBRARY_PATH="$(./astroncia/pfs.py)"
export LD_LIBRARY_PATH
exec /usr/bin/python3 ./astroncia_iptv.py "$@"
exit "$?"
